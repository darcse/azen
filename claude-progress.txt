2026-04-21
- Next.js(App Router + TypeScript + TailwindCSS) 초기 세팅 완료 (현재 폴더 기준)
- 기존 문서 파일(CLAUDE.md, HARNESS.md, CONVENTIONS.md, init.json) 보존하며 부트스트랩 진행
- Supabase 패키지 설치: @supabase/supabase-js, @supabase/ssr
- Supabase 클라이언트/서버 유틸 생성: lib/supabase/client.ts, lib/supabase/server.ts
- 인증 세션 갱신 및 /admin 보호 로직 추가: lib/supabase/middleware.ts, middleware.ts
- .env.local 템플릿 생성 (URL/ANON KEY 플레이스홀더)
- Pretendard CDN 전역 적용: app/globals.css, layout 메타데이터 정리
- 컨벤션 기반 폴더 생성: app/(public), app/admin, components/ui, components/layout, components/features, hooks, types
- 공통 타입 파일 생성: types/index.ts (Product, Category, ProductImage)
- 검증: npm run dev 실행 성공 (localhost:3001 기동 확인)
- INIT-000 진행: Git 저장소 초기화(main), origin 연결(https://github.com/darcse/azen.git), 초기 커밋/푸시 완료
- 원격 검증: origin/main 해시(0cfa50dc7558207071b136bf82fbe6a30cdc1b64) 확인
- Vercel CLI 배포 시도 결과, 인증 토큰 불일치로 중단 (vercel login 또는 유효 토큰 필요)
- 사용자 수행으로 Vercel 배포 완료 확인: https://azen-gilt.vercel.app
- INIT-000 완료 처리 (feature_list/init.json의 INIT-000 passes=true)
- INIT-001 보완 작업: CONVENTIONS 기준 라우트 파일 골격 생성 (about/products/[id]/service/admin/login)
- types/index.ts를 AzenCategory/AzenProduct/AzenProductImage + snake_case 필드로 재정의
- .env.local 템플릿을 플레이스홀더 값으로 재설정
- 검증: npm run lint 통과
- 골격 페이지 placeholder 반영: about/products/products/[id]/service/admin/admin products new/admin products [id] edit/login
- 동적 라우트 placeholder에 params.id 출력 추가 (제품 상세/제품 수정)
- 검증: npm run lint 통과
- INIT-001 완료 처리: feature_list/init.json의 INIT-001 passes=true
- INIT-002 작업: Supabase SQL 스크립트 추가 (supabase/init-002.sql)
- 포함 내용: azen_categories/azen_products/azen_product_images 테이블, 인덱스, updated_at 트리거, RLS 정책, 카테고리 시드 데이터
- 사용자 수동 검증 완료 기준으로 INIT-002 완료 처리 (feature_list/init.json의 INIT-002 passes=true)
- INIT-003 작업: Storage 버킷/정책 SQL 추가 (supabase/init-003.sql)
- 포함 내용: product-images public 버킷 upsert, 공개 읽기 정책, admin 쓰기(insert/update/delete) 정책, MIME/용량 제한
- INIT-003 검증 체크리스트 문서 추가 (supabase/init-003-checklist.md)
- INIT-003 완료 처리: feature_list/init.json의 INIT-003 passes=true
- 테마 시스템 구현: light/dark/auto + localStorage(theme) + 시스템 테마(matchMedia) 연동 훅 추가 (hooks/useTheme.ts)
- 폰트/토큰 반영: globals.css에 Manrope/Pretendard CDN import 및 CSS 변수 기반 라이트/다크 토큰 구성
- Tailwind 확장: tailwind.config.ts 추가(darkMode class, 색상 토큰, fontFamily, borderRadius)
- UI 반영: ThemeToggle(lucide-react Sun/Moon/Monitor 순환 버튼), Header(로고/네비/모바일 메뉴/토글) 추가
- 레이아웃 반영: 깜빡임 방지 초기화 스크립트 + Header 포함하도록 app/layout.tsx 갱신
- 검증: npm run lint 통과 (dev는 기존 3001 서버 실행 중 상태 확인)
- UX-001 점검 완료: 테마 토큰/3단계 토글/auto 모드/localStorage/반응형 헤더 구조 충족 확인, feature_list/ux.json passes=true 반영
- MEDIA-001 구현: 관리자 제품 이미지 등록 폼 추가 (파일 업로드 탭 + URL 입력 탭)
- 구현 상세: Storage(product-images) 업로드 후 public URL을 azen_product_images insert, URL 직접 입력 insert
- 예외 처리: 이미지 외 파일 업로드 차단, 잘못된 URL 형식 검사, 상세 갤러리 broken image 대응
- 제품 상세 페이지에 이미지 갤러리 연동 (product_id 기준 조회)
- MEDIA-001 완료 처리: feature_list/media.json passes=true 반영
- 검증: npm run lint 통과
- AUTH-001 구현: 로그인 폼 추가 (email/password + signInWithPassword + redirect 쿼리 지원)
- 관리자 페이지 로그아웃 버튼 추가 (signOut 후 /login 이동)
- 기존 middleware(/admin 보호)와 연계하여 미로그인 리다이렉트/로그인 후 진입 흐름 완성
- 검증: npm run lint 통과
- AUTH-001 완료 처리: feature_list/auth.json passes=true 반영
- 로그인 화면 콘솔 에러 대응: layout의 인라인 script 태그를 next/script(beforeInteractive)로 교체
- 하이드레이션 불일치 대응: useTheme 초기값을 auto로 통일하여 SSR/CSR 렌더 불일치 제거
- 검증: npm run lint 통과
- PAGE-001 구현: 메인 홈(/)에 히어로/카테고리/특징 벤토 섹션 구성 (정적 placeholder)
- 히어로 영역: 좌측 카피 + CTA 2개, 우측 Unsplash 산업용 이미지(next/image) 적용
- 카테고리 카드: 필터/전기·유공압/교체시공 3개 링크 카드 구성
- 특징 섹션: lucide-react 아이콘 기반 4개 강점 카드, md:grid-cols-4 + col-span 조합 적용
- next.config.ts에 images.unsplash.com remotePatterns 허용 추가
- PAGE-001 완료 처리: feature_list/page.json passes=true 반영
- 검증: npm run lint 통과
- UX-001 다크 토큰 업데이트: globals.css .dark를 Obsidian Glass 값(background/surface/elevated/primary/secondary/success/foreground/muted/border)으로 교체
- tailwind.config.ts 확장: surface/elevated/secondary/success 컬러 토큰 추가
- app/page.tsx 다크 스타일 반영: 카드 glass 스타일(bg #1c1c1e, border-white/5, rounded-2xl, backdrop-blur-xl), 텍스트 다크 톤, primary 버튼 active scale 효과
- Header 다크 스타일 반영: 헤더/드롭다운/모바일 메뉴에 surface glass 스타일(bg #0e0e10/80, backdrop-blur-xl, border-white/5) 적용
- 검증: npm run lint 통과
- globals.css @layer utilities 추가: glass-card / glass-header 유틸 클래스 정의
- 카드/헤더 컴포넌트 인라인 Glass 다크 클래스 제거 후 glass-card, glass-header 사용으로 통일
- CONVENTIONS.md에 Glass 유틸 사용 기준 추가 (카드=glass-card, 헤더/네비=glass-header)
- 검증: npm run lint 통과
- PAGE-001 섹션 개선: '제품 카테고리'→'Products', 'AZEN의 강점'→'AZEN Powers'로 섹션명 변경
- Products 섹션을 커스텀 캐러셀로 전환: 3초 autoplay, 좌우 화살표, 데스크탑 3개/모바일 1개 카드 노출
- Products 카드 6개 구성: Unsplash 이미지 + 카테고리명/설명 + /products/[id] 링크 적용
- AZEN Powers 섹션 개선: 모바일 2열/데스크탑 4열 균등 카드, 배경 이미지+다크 오버레이+아이콘+텍스트, /about 링크
- hover 효과 반영: AZEN Powers 카드 오버레이 밝기 완화(group-hover)
- 검증: npm run lint 통과
- PAGE-001 보완: Products/Powers 일부 더미 이미지 깨짐 URL을 정상 URL로 교체
- Products 카드 이미지 높이 상향: h-44 -> h-[22rem] (약 2배)
- AZEN Powers 카드 높이 상향: h-56 -> h-[28rem] (약 2배)
- 캐러셀 전환 개선: 1칸 단위 이동 + translate 트랙 애니메이션(duration-700 ease-out), autoplay 5초로 조정
- 검증: npm run lint 통과
- Products 캐러셀 모바일 스와이프 추가: 터치 시작/종료 좌표 기반 임계치(40px)로 prev/next 이동
- AZEN Powers 카드 인터랙션 강화: hover 시 카드 리프트/글로우 그림자, 이미지 scale, 오버레이 밝기 상승, 아이콘 미세 이동
- 검증: npm run lint 통과
- 히어로 문구 수정: 슬로건/서브카피 최신 문안 반영, 슬로건 첫 줄(멈추지 않는 공정)에 포인트 컬러 적용
- CTA 문구 수정: '제품 보러가기' -> '현장 진단 신청'
- 섹션 타이틀 수정: 'AZEN Powers' -> 'Core Values'
- 히어로 이미지 교체: Factory filter/HVAC technician 톤의 Unsplash 이미지로 변경
- 검증: npm run lint 통과
- PAGE-002 구현: components/layout/Footer.tsx 추가 및 app/layout.tsx에 푸터 연결
- 푸터 구성: SVG 로고(현재색상 fill), 회사 정보(주소/대표자/사업자번호), 카피라이트 배치
- 반응형 레이아웃: 모바일 세로 스택 중앙정렬, 데스크탑 3열 justify-between
- 스타일: glass-header 유틸 + border-t border-border로 본문과 구분, 라이트/다크 대응
- PAGE-002 완료 처리: feature_list/page.json passes=true 반영
- 검증: npm run lint 통과
- UX-001 Header 개선: 텍스트 로고를 SVG 로고(footer 동일)로 교체, 높이 h-7(약 28px), 홈 링크 적용
- 메뉴 레이아웃 조정: 로고+메뉴를 좌측 그룹으로 묶고 메뉴 폰트 text-base로 상향
- 메뉴 hover 강화: 텍스트 강조 + 하단 border 하이라이트 + 배경 하이라이트 적용
- 드롭다운 이탈 버그 수정: 패널 mt 제거 및 top-full 연결, pt-2 브리지로 hover 끊김 방지
- 테마 토글 버튼 스타일 변경: border 제거, 아이콘 중심 버튼으로 단순화
- 검증: npm run lint 통과
- Header 접근성 보완: 드롭다운에 aria-expanded/aria-controls 추가
- 키보드 제어 추가: Enter/Space로 열기·닫기, Escape로 닫기, blur 시 외부 포커스 이동이면 자동 닫기
- hover/키보드 통합 오픈 상태 관리(openDropdown)로 데스크탑 사용성 안정화
- 검증: npm run lint 통과
- 헤더 스타일 미세조정: 로고-메뉴/메뉴 간격 확대, 인터랙션을 폰트 굵기+언더라인 중심으로 단순화
- 언더라인 포인트 컬러 적용 및 underline offset 증가, 다크 모드 드롭다운 대비 개선
- 푸터 정보 업데이트: 로고-정보 간격 40px, 주소/대표자 변경, 카피라이트 하단 이동 및 연도 2026 반영
- PAGE-001 히어로 리디자인: 우측 이미지 컬럼 제거, 배경 이미지를 CSS background-image + dark overlay로 전환
- 검증: npm run lint 통과
- PAGE-001 히어로 레이아웃 재조정: full-width 섹션으로 분리, 본문 max-w 컨테이너와 분리 렌더링
- 히어로 정렬 기준: 내부 콘텐츠를 max-w-7xl mx-auto px-8로 유지, min-h-[600px] 적용
- 히어로 배경 개선: 공장 내부 톤 Unsplash 이미지로 교체, cover/center + dark overlay 유지
- 검증: npm run lint 통과
- /login Vercel prerender 에러 수정: app/login/page.tsx에 `export const dynamic = "force-dynamic"` 추가
- 목적: 빌드 시 /login 정적 프리렌더 충돌 회피 및 동적 렌더 강제
- 검증: npm run lint 통과
- ADMIN-001 구현: `app/admin/page.tsx`를 서버 컴포넌트 기반 제품 목록 테이블로 확장 (azen_products + azen_categories 조인 조회)
- 관리자 목록 컬럼 구성: 제품명/카테고리/공개여부 뱃지/정렬순서/등록일/수정·삭제 액션 버튼
- 상단 액션 추가: `제품 등록` 버튼(`/admin/products/new`) 및 기존 로그아웃 버튼 유지
- 빈 상태/에러 상태 UI 추가: glass-card 스타일 메시지 및 안내 문구 반영
- 삭제 플로우 구현: `components/features/AdminDeleteButton.tsx` 생성, confirm 다이얼로그 후 server action 호출로 제품 삭제
- DB cascade 확인: `supabase/init-002.sql`의 `azen_product_images.product_id ... on delete cascade` 설정 기반 연관 이미지 자동 삭제
- ADMIN-001 완료 처리: `feature_list/admin.json`의 ADMIN-001 passes=true 반영
- ADMIN-002 구현: `app/admin/products/new/page.tsx`를 관리자 제품 등록 페이지로 전면 교체 (서버 액션 기반 저장 + `/admin` 리다이렉트)
- 입력 필드 구성: 제품명(필수), 카테고리(서브카테고리 목록), 간단설명, 상세 HTML, 공개여부, 정렬순서
- 대표 이미지 처리: 파일 업로드(Storage `product-images`) 또는 URL 직접 입력 탭 전환 지원, 결과를 `thumbnail_url`로 저장
- 추가 이미지 처리: 파일 다중 업로드 또는 URL 복수 입력 지원, `azen_product_images`에 다건 insert
- UI 구성: 상단 `← 목록으로` 버튼(`/admin`), glass-card 기반 폼, lucide-react 아이콘 버튼 적용
- 유효성/에러 처리: 제품명/카테고리 필수 검증, URL 형식 검사, 서버 액션 에러 메시지 노출
- ADMIN-002 완료 처리: `feature_list/admin.json`의 ADMIN-002 passes=true 반영
- ADMIN-003 구현: `app/admin/products/[id]/edit/page.tsx`에 기존 제품/카테고리/이미지 조회 및 수정 폼 연동
- 존재하지 않는 제품 ID 접근 시 `/admin`으로 리다이렉트 처리
- 수정 폼 구성: ADMIN-002와 동일 구조(제품명/카테고리/설명/공개여부/정렬/대표이미지/추가이미지/상세 HTML)
- 기존 이미지 관리: 목록 노출 + 개별 삭제 버튼(`AdminImageDeleteButton`)으로 DB row 삭제 및 Storage 파일 동시 제거
- 신규 이미지 추가: 파일 업로드 또는 URL 입력으로 `azen_product_images` 추가 insert 지원
- 저장/삭제 액션: server action으로 `azen_products` update 후 `/admin` 리다이렉트, 제품 삭제(confirm) 시 `azen_products` delete(cascade 적용)
- 신규 컴포넌트 추가: `components/features/AdminProductEditForm.tsx`, `components/features/AdminImageDeleteButton.tsx`
- ADMIN-003 완료 처리: `feature_list/admin.json`의 ADMIN-003 passes=true 반영
- ADMIN-004 구현: `app/admin/page.tsx`에 URL 쿼리(`search`, `category`, `sort`) 기반 서버 필터링/정렬 추가
- 검색 기능: `azen_products.name ilike` 조건으로 제품명 텍스트 검색 적용
- 카테고리 필터: `azen_categories` 서브카테고리 목록 드롭다운 구성 및 `category_id` 조건 필터 적용
- 정렬 기능: 이름 오름/내림, 등록일 최신/오래된순 드롭다운 선택에 따른 쿼리 정렬 적용
- UI 구성: 목록 상단에 검색 input + 카테고리 select + 정렬 select를 한 줄 form으로 배치
- 검색 결과 빈 상태 문구를 필터링 맥락에 맞게 개선 (`조건에 맞는 제품이 없습니다`)
- ADMIN-004 완료 처리: `feature_list/admin.json`의 ADMIN-004 passes=true 반영
- ADMIN-005 구현: 관리자 전용 레이아웃 분리 (`app/admin/layout.tsx`) 및 AdminHeader/AdminFooter 구성
- 전역 공개 레이아웃 분리: `ConditionalHeader` 추가, `ConditionalFooter` 확장으로 `/admin` 및 `/login`에서 공개 Header/Footer 비노출 처리
- 관리자 GNB 추가: `components/layout/AdminHeader.tsx` (좌측 로고→`/admin`, 중앙 메뉴 `제품 등록`/`메인 관리`, 우측 ThemeToggle + 로그아웃)
- 로그아웃 동작: AdminHeader 내 server action으로 `supabase.auth.signOut()` 후 `/login` redirect 구현
- 관리자 푸터 추가: `components/layout/AdminFooter.tsx`에 카피라이트 중앙 정렬(`© 2025 AZEN. All rights reserved.`)
- `/admin/main` placeholder 생성: `app/admin/main/page.tsx` (ADMIN-006 예정 안내)
- ADMIN-005 완료 처리: `feature_list/admin.json`의 ADMIN-005 passes=true 반영
- ADMIN-006 구현: `/admin/main` 메인 캐러셀 슬롯 관리 페이지 구성 (슬롯 1~6 카드 + 제품 드롭다운 + 비움 옵션)
- 신규 컴포넌트: `components/features/AdminMainCarouselForm.tsx` (server action 연동 저장 버튼/성공·에러 안내)
- 데이터 연동: `azen_main_carousel` 현재 값 fetch 후 슬롯별 초기값 표시, `azen_products` 전체 목록으로 선택 옵션 구성
- 저장 처리: server action에서 슬롯 1~6 일괄 upsert(`onConflict: slot`)로 `azen_main_carousel` 업데이트
- 홈 캐러셀 연동: `app/page.tsx`에서 더미 데이터 제거, `azen_main_carousel -> azen_products` 조인 데이터로 Products 구성
- 슬롯 비움 처리: `product_id is null` 슬롯은 홈 캐러셀에서 자동 제외
- 캐러셀 클라이언트 분리: `components/features/HomeProductsCarousel.tsx`로 인터랙션(autoplay/스와이프/화살표) 이전
- ADMIN-006 완료 처리: `feature_list/admin.json`의 ADMIN-006 passes=true 반영
- PRODUCT-001 구현: 공개 제품 목록 페이지 `/products` (히어로 + 좌측 카테고리 사이드바 + 카드 그리드)
- 데이터: `azen_categories` 계층(메인/서브) 사이드바 링크, `azen_products`에서 `is_published=true`만 조회, `?category=slug`로 서브 또는 메인(하위 전체) 필터
- UI: Unsplash 산업 배경 히어로 + 다크 오버레이, glass-card, 제품 카드는 캐러셀과 동일한 hover(scale/translate/shadow), 썸네일 없을 시 `/hero-factory.png`, 설명 `line-clamp-2`, 클릭 시 `/products/[id]`
- 빈 상태: 필터 결과 없음 / 잘못된 slug / 전체 없음 각각 안내 문구
- GNB: `Header.tsx` 드롭다운에 `전체 제품`, `필터 전체`(`?category=filter`), `전기/유공압 전체`(`?category=electric`) 및 기존 서브 `?category=slug` 링크 정리
- 신규 컴포넌트: `components/features/ProductCard.tsx`, `components/features/ProductsCategorySidebar.tsx`
- PRODUCT-001 완료 처리: `feature_list/product.json`의 PRODUCT-001 passes=true 반영
- 검증: npm run build 통과
- next.config.ts `images.remotePatterns` 확장: `imageRemotePatterns` 배열로 정리하고 외부 이미지 URL 입력 대비 호스트 추가
- 추가 호스트: `postfiles.pstatic.net`(http/https), `i.imgur.com`/`imgur.com`/`m.imgur.com`, `res.cloudinary.com` 및 `**.cloudinary.com`, `lh3~lh6.googleusercontent.com` 및 `**.googleusercontent.com`, `imagedelivery.net`(Cloudflare Images), `picsum.photos`/`placehold.co`/`via.placeholder.com`, `raw.githubusercontent.com`/`user-images.githubusercontent.com`/`avatars.githubusercontent.com`
- 기존 유지: `images.unsplash.com`, `www.capfil.co.kr`, `www.ecsfilter.com`(/upload), `www.daewonvalvetech.com`(/wp-content)
- 검증: npm run build 통과
- PRODUCT-001 UI 개편: `/products` 좌측 사이드바 제거, 히어로 직후 상단 필터 바(`ProductsFilterBar`)로 교체
- 필터 바: 좌측 GET 폼 `?search=`(제품명) + 선택 카테고리 유지용 hidden, 우측 `전체` 및 필터·전기 하위 서브칩(`?category=slug`), 활성은 `bg-primary`+`text-white`, 모바일 카테고리 가로 스크롤
- 서버: `name`에 `ilike` 검색, `search`·`category` 쿼리 조합, 검색 결과 없을 때 안내 문구 분기
- 그리드: `md:grid-cols-3` `lg:grid-cols-4` 전폭
- `ProductsCategorySidebar.tsx` 삭제, `ProductsFilterBar.tsx` 추가
- 검증: npm run build 통과
- `/products` 필터 칩 그룹 분리: `resolveFilterBarGroup`으로 URL `category`가 `electric`·`electric_parts`·`hydraulic`이면 전기/유공압 줄만(전체=`electric` + 서브 2개), 그 외(필터 서브·`filter`·없음·`service` 등)는 필터 줄만(전체=`filter` + 서브 4개)
- `ProductsFilterBar`: `filterSubcategories`/`electricSubcategories`/`activeBarGroup` props, 그룹별 `전체`는 각각 `?category=filter` / `?category=electric`
- 제품 그리드: `lg:grid-cols-4` → `lg:grid-cols-3`, `gap-6` 유지
- 검증: npm run build 통과
- `/products` 전면 재작성: 필터·전기/유공압 제품군 완전 분리(GNB `?category=`로만 진입, 없거나 잘못된 값은 `?category=filter`로 redirect)
- 서버: `lib/products-catalog.ts`로 그룹·slug 정의, 해당 그룹 `category_id`만 `azen_products` 조회(`category:azen_categories(name, slug)`), `ProductsCatalogClient`에 전달
- 클라이언트 `ProductsCatalogClient`: 제품명 검색 onChange 실시간 필터, 칩은 그룹 내 `전체`+서브만(`useState`), 활성 `bg-primary`
- 레이아웃: 히어로 `min-h-[200px]`·Unsplash `background-image`+`bg-black/60`, 본문 `max-w-7xl` `px-8`, 그리드 `md:grid-cols-2` `lg:grid-cols-3` `gap-6`
- `ProductCard`: 이미지 `aspect-[3/2]`, hover scale·translate 전 구간 적용
- `ProductsFilterBar.tsx` 삭제, `Header`/홈 CTA 제품 링크에 `?category=filter`·`electric` 정리
- 검증: npm run build 통과
- `ProductCard` 이미지 영역 개선: `aspect-[4/3]` 고정, `object-contain`+`object-center`, 배경 `bg-elevated`/`dark:bg-[#1c1c1e]`, `inset-4`로 패딩, 썸네일 없을 때 동일 배경 + 중앙 `lucide-react` `Image` 아이콘( `/hero-factory.png` 폴백 제거)
- 검증: npm run build 통과
- `ProductCard` 이미지 영역 재조정: 썸네일 있을 때 `aspect-[4/3]`+`object-cover`로 영역 꽉 채움, 패딩·배경색 없음; 썸네일 없을 때만 `bg-elevated`/`dark:bg-[#1c1c1e]`+중앙 `Image` 아이콘
- 검증: npm run build 통과
- `ProductCard`를 홈 `HomeProductsCarousel` 카드와 스타일 통일: `glass-card`+동일 border/bg/hover(`md:hover:-translate-y-1`, 오버레이, 이미지 `md:group-hover:scale-105`), 이미지 `aspect-[4/3]`+`overflow-hidden`+`object-cover`, 텍스트 `p-4`·`dark:bg-[#111114]`, 설명 `min-h-[2.75rem]`·`leading-[1.375rem]`, 카테고리 `lowercase`+`text-primary`
- 검증: npm run build 통과
- `ProductCard` 캐러셀 카드와 완전 통일: 이미지 `h-[22rem]`(고정 높이), 콘텐츠 `p-5 lg:p-6`, 카드 wrapper에서 `flex h-full flex-col` 제거
- `/products` 타이틀 영역 전면 개편: full-width CSS `background-image`(필터/전기 그룹별 Unsplash 산업 이미지)+`bg-black/60` 오버레이, `min-h-[220px]` 세로 중앙 정렬, 흰색 제목·설명
- `/products` 검색+필터 바 개편: `max-w-7xl mx-auto px-8 py-6`, 검색 input `flex-1`+`bg-elevated px-4 py-2 rounded-lg`, 카테고리 칩 `rounded-full px-4 py-1.5 text-sm`
- `/products` 카드 그리드 섹션: `max-w-7xl mx-auto px-8 py-8`, `md:grid-cols-2 lg:grid-cols-3 gap-6` 유지
- 검증: npm run build 통과
- `/products` 타이틀 영역 세부 조정: `min-h-[220px]` → `min-h-[300px]`, 내부 패딩 `py-12` → `py-16`
- 검색+필터 바 레이아웃 개편: 두 컬럼 `justify-between` 구조 → `flex-wrap gap-3` 단일 행, 검색창 `w-64` 고정폭, 칩 그룹 인접 배치
- 검색창 배경 명시: `bg-elevated dark:bg-[#1c1c1e]`로 토큰 통일
- 카드 그리드 오버플로 수정: `page.tsx`의 불필요한 `pt-8` 래퍼 div 제거, `ProductsCatalogClient` 직접 렌더링
- 검증: npm run build 통과
- `/products` 컨테이너 너비 통일: `max-w-7xl px-8` → `max-w-6xl px-4` (Header.tsx와 동일한 컨테이너)
- `/products` 검색창 색상 hex 직접 적용: background `#1c1c1e`, color `#fefbfe`, border `rgba(255,255,255,0.05)`, placeholder `#636366`
- 검증: npm run build 통과
- `ProductCard` 이미지 영역: `aspect-[4/3]`+`overflow-hidden` 유지, 썸네일·플레이스홀더 모두 하단 `h-16` `bg-gradient-to-t from-black/30 to-transparent` 오버레이(`pointer-events-none`)
- 검증: npm run build 통과
- `tailwind.config.ts` `primary-foreground: var(--primary-foreground)` 추가 (기존 토큰은 이미 연결되어 있었음)
- `ProductCard.tsx` / `ProductsCatalogClient.tsx` hex 잔존 여부 재확인 → 이미 모두 Tailwind 토큰으로 교체 완료 상태, 변경 없음
- `globals.css` `:root`에 `--elevated: #f1f5f9`, `--surface: #f8fafc`, `--success: #16a34a`, `--secondary: #3b82f6`, `--primary-foreground: #ffffff` 추가
- `globals.css` `.dark`에 `--primary-foreground: #ffffff` 추가
- `ProductCard.tsx` 이미지 비율 `aspect-[4/3]` → `aspect-[3/2]`, 텍스트 패딩 `p-4` → `p-5`
- `ProductCard.tsx` 이미지 영역 `aspect-[3/2]` → `h-64` 고정 높이로 변경 (카드 크기 확대)
- `ProductCard.tsx` 이미지 영역 `h-64` → `h-[22rem]` (홈 캐러셀 `HomeProductsCarousel.tsx`와 동일하게 통일)
- `ProductCard.tsx` 인터랙션 캐러셀 카드와 통일: `glass-card`, `md:hover:shadow`, 내부 오버레이 div(`opacity-0→group-hover:opacity-100`), 이미지 `md:group-hover:scale-105`, 텍스트 영역 `z-[2]`
- `Header.tsx` GNB 수정: '필터 전체'/'전기유공압 전체' 드롭다운 항목 제거, '필터' 클릭 → `/products?category=air_handling`, '전기/유공압' 클릭 → `/products?category=electric_parts` (데스크탑 button→Link, 모바일 p→Link)
- `Header.tsx` GNB 링크 수정: '필터' → `?category=filter`, '전기/유공압' → `?category=electric` (데스크탑+모바일 동일)
- `ProductsCatalogClient.tsx` 그룹 판단 로직 명시화: `initialSelectedSub`를 FILTER_SUB_SLUGS/ELECTRIC_SUB_SLUGS 기준으로 단순화, "전체" 시 useMemo에서 해당 그룹 서브슬러그 Set으로 명시적 필터링
- `app/layout.tsx` 래퍼 div `min-h-full` → `min-h-screen` (제품 적을 때도 푸터 하단 고정)
- `ProductsCatalogClient.tsx` 검색/필터 바 `pt-8` 추가 (타이틀 영역과 간격)
- `ProductsCatalogClient.tsx` 모바일 필터 select 추가: `md:hidden` select(전체+카테고리 옵션, ChevronDown 아이콘), 검색창과 한 줄 배치; 데스크탑 칩 `hidden md:flex`로 분리
- `ProductCard` + `ProductsCatalogClient` 라이트/다크 대응: hex 하드코딩 → Tailwind 토큰(bg-elevated, text-foreground, text-muted-foreground, text-primary, border-border, bg-primary) 전환, 검색 input inline style 제거
- 검증: npm run build 통과
- `/products` 전면 재구성: 서버 컴포넌트(page.tsx) + 클라이언트 컴포넌트(ProductsCatalogClient) 분리
- 타이틀 영역: `min-h-[300px]` CSS background-image + `bg-black/60` 오버레이, `items-end px-4 py-12`, 그룹별 제목·설명
- 검색+필터 바: `max-w-6xl px-4 py-6`, 검색 input `w-64` inline style(bg `#1c1c1e`, color `#fefbfe`, border `rgba(255,255,255,0.05)`), 카테고리 칩 `rounded-full px-4 py-1.5`(선택 `bg-[#0A84FF]`, 미선택 `bg-[#1c1c1e] text-[#acaaad]`)
- 카드 그리드: `max-w-6xl px-4 py-8`, `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- `ProductCard` 전면 개편: `rounded-2xl border border-white/5 bg-[#1c1c1e]`, 이미지 `aspect-[4/3]`+img 태그, 카테고리 `text-[#0A84FF]`, 제품명 `font-bold text-[#fefbfe]`, 설명 `text-[#acaaad]`, hover `transition-transform -translate-y-1`
- 검증: npm run build 통과
- `globals.css` `@layer base`에 `.dark input` / `.dark input::placeholder` 스타일 추가 (Tailwind base가 덮어쓰던 다크모드 input 색상 수정)
- `ProductsCatalogClient.tsx` 검색 input 인라인 style 제거, className 토큰(`border-border bg-background text-foreground placeholder:text-muted-foreground`)만으로 정리
- 검증: npm run build 통과
- `ProductCard` 이미지 영역: 하단 그라디언트 오버레이 유지, 높이를 홈 캐러셀과 동일하게 `h-[22rem]`으로 복구(`aspect-[4/3]` 제거)
- 검증: npm run build 통과
- `/products` 제품 목록 정렬: `created_at` 오름차순(먼저 등록한 순), 보조 `sort_order` 오름차순
- 검증: npm run build 통과
2026-04-22
- PRODUCT-002 구현: `app/(public)/products/[id]/page.tsx` 제품 상세(공개 `is_published=true` 단건 조회, 카테고리·추가 이미지 nested select, 미존재/비공개 `notFound()`)
- 레이아웃: `max-w-7xl mx-auto px-8 py-12`, `lg:grid-cols-2` 좌우 분할, 좌 `glass-card`+우 `bg-elevated rounded-2xl`
- 갤러리: `thumbnail_url` + `azen_product_images`(sort_order) URL 병합·중복 제거, 메인 `aspect-[4/3]`·`object-contain`·`bg-elevated`·`rounded-2xl`, 썸네일 가로 스크롤·라벨로 라디오 전환(CSS 인접 형제), 이미지 없을 때 `bg-elevated`+`lucide-react` `Image` 플레이스홀더
- 우측: 카테고리명(`text-primary` 소형)·제품명(`text-2xl font-bold`)·간단 설명·구분선·상세 HTML(`dangerouslySetInnerHTML`), 상단 별도 타이틀 영역 없음
- '목록으로 돌아가기': `next/script`(afterInteractive)로 `history.back()` 연결(단일 `page.tsx` 유지 목적)
- 검증: npm run build 통과
- 제품 상세 UI 개편(`app/(public)/products/[id]/page.tsx`): 상단 `max-w-7xl px-8 pt-8`에 `ArrowLeft`+제품명 한 줄(`flex items-center gap-3`), `history.back()` 동일 스크립트(`data-product-history-back`)
- 브레드크럼: `text-sm text-muted-foreground`, 부모(`parent_id`로 `azen_categories` 추가 조회)·현재 카테고리는 `/products?category=` 링크, 마지막은 제품명 텍스트
- 본문: `lg:grid-cols-2` `gap-8` `py-8` 구역에 갤러리 유지 + 우측 기본정보(카테고리 `text-primary text-sm`, 제품명, 설명, 구분선, 스펙 placeholder「스펙 정보 준비 중」)
- 하단: `max-w-7xl px-8 py-8`에 구분선·「제품 상세」소제목·`content` HTML만 노출(없으면 섹션 미렌더)
- 검증: npm run build 통과
- 제품 상세 레이아웃 정리(`app/(public)/products/[id]/page.tsx`): 본문 전체를 단일 `max-w-7xl mx-auto px-8` 래퍼로 통일(Header와 동일 너비·하단 상세 동일 좌우 여백)
- 브레드크럼: 타이틀 제품명 시작선에 맞추기 위해 `ml-12`(뒤로 버튼+`gap-3` 폭) 적용
- 갤러리: 메인 영역 `bg-elevated`/라운드 제거·`aspect-[4/3]`+`object-contain`만; 썸네일 `size-12` 정사각형·`border-2 border-border`·`:has(#pdg-n:checked)`로 선택 시 `border-primary`
- 우측 기본정보: `glass-card`/`bg-elevated`/패딩 카드 제거(텍스트만), 그리드 `items-start`로 콘텐츠 높이 자연 배치
- 검증: npm run build 통과
- 제품 상세(`app/(public)/products/[id]/page.tsx`) 오버플로·간격: `main`에 `overflow-x-hidden`, 섹션 공통 `w-full max-w-7xl mx-auto px-8` 3분할(타이틀+브레드 / 그리드 / 상세)
- 타이틀 블록 `pb-8 pt-8`, 그리드 블록 `pt-8 pb-8`, 상세 블록 `pt-12 pb-8`; 갤러리 `w-full`→`max-w-full`, 열에 `min-w-0`로 그리드 가로 넘침 완화
- 검증: npm run build 통과
- 제품 상세(`app/(public)/products/[id]/page.tsx`) 전면 재작성: CSS 라디오 기반 갤러리 제거, `components/features/ProductGallery.tsx`(use client, useState 기반 썸네일 클릭 전환)로 분리
- 레이아웃: 타이틀 바(`pt-8 pb-2` ArrowLeft+제품명), 브레드크럼(`pb-8 ml-10`), 좌우 그리드(`grid lg:grid-cols-2 gap-12 pb-16`), 하단 상세(`pt-12 pb-16`)
- 갤러리: `w-full aspect-[4/3] object-contain` 메인 이미지, `w-20 h-20` 썸네일 strip(선택 `border-primary`), 이미지 없을 때 `aspect-[4/3]` 점선 플레이스홀더
- `<style>` 태그 + CSS radio 패턴 완전 제거, `next/script` `history.back()` 유지
